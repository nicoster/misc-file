#include <stdio.h>

void main()
{
    double z;
    struct
    {
        double x;
    } a;
    scanf("%f", &z);

	// uncomment the following line to force load floating point library.
 //   a.x = z;
}
